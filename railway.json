{
  "build": {
    "buildCommand": "npm install -g @bcharleson/linkedincli && pip install fastapi uvicorn"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "healthcheckPath": "/health"
  }
}
